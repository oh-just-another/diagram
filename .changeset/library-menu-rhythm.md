---
"@oh-just-another/react-ui": patch
---

The shape library list follows the menu rhythm: 40-px rows with the menu row padding / gap / font, section titles styled like `du-menu-group-title`, sections inset by `--du-menu-pad`.
