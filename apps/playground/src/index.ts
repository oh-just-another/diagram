/**
 * Package entry for the private playground app. Re-exports the editor so the
 * demo (and any in-repo tooling) can pull it from a single local name.
 * Not published — import from `@oh-just-another/editor` directly in real apps.
 */
export * from "@oh-just-another/editor";
