---
"@oh-just-another/react-ui": minor
"@oh-just-another/editor": minor
---

Undo / redo buttons in the bottom bar, left of the zoom controls: `HistoryControls` (react-ui) disables each side when its stack is empty or in read-only mode; the `Editor` mounts it by default, `hideHistoryControls` removes it.
