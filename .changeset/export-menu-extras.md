---
"@oh-just-another/editor": minor
---

`Diagram` gains `renderExportMenuExtras` — extra rows inside Board › Export after the built-in PNG / SVG entries, so hosts add their export formats to the same submenu instead of a second "Export as".
