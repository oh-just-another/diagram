---
"@oh-just-another/react-ui": patch
---

Accessibility: palette items are focusable (`tabIndex=0`), named `<name>, <hint>, draggable` and place their shape on Enter / Space; the selection toolbar is a `role="toolbar"` named "Selection" with an `aria-describedby` summary of a multi-selection (`describeSelection`).
