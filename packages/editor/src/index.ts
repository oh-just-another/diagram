// Public API of `@oh-just-another/editor`.
//
// Skeleton — the editor component, capability detection, and WASM/worker
// wiring are moved in here from `apps/diagram` in the extraction step.
export {};
