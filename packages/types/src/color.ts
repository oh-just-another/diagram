/**
 * CSS color string: `#fff`, `#ffffff`, `#ffffffff`, `rgb(...)`, `rgba(...)`,
 * `hsl(...)`, or one of the supported named colors.
 *
 * Validation, parsing and manipulation live in @oh-just-another/math/color.
 * Stored as plain string so it composes directly with Canvas2D and SVG.
 */
export type Color = string;
