---
"@oh-just-another/serialization": minor
---

Add `sceneJsonSchema()` — JSON Schema (draft-07) of the scene wire document, generated on the fly from the zod schema. For LLM structured output and external validators.
