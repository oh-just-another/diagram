---
"@oh-just-another/scene": minor
---

Export `FALLBACK_SCENE_WIDTH` / `FALLBACK_SCENE_HEIGHT` — the default canvas dimensions for a scene with no explicit viewport size. Shared by the import and export adapters.
