---
"@oh-just-another/state": patch
---

Resize handles are now centred exactly on the selection frame's corners and edge midpoints (`HANDLE_OUTSET` defaults to 0). Group resize / rotate chrome is no longer hit-testable or drawn when nothing in the selection is manipulable (e.g. a locked group).
