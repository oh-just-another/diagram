---
"@oh-just-another/mcp": minor
---

New package: MCP server (`oja-mcp` binary) exposing scene tools over stdio for LLM agents — create/load/get scene, add/update/remove elements, add_link, query_scene, export_svg/export_png, import_mermaid, get_scene_schema.
