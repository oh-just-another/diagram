# @oh-just-another/editor

Drop-in diagram editor as a single React component. Auto-detects renderer /
WASM / worker capabilities and exposes a programmatic editor API.

> Work in progress — the component is being extracted from the demo app into
> this package. API documented here once the extraction lands.
