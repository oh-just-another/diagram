---
title: "React: drop-in"
sidebar_position: 1
---

import ExampleEditor from "@site/src/components/ExampleEditor";

One component, zero config — toolbar, panels, hotkeys, undo, persistence-ready scene model included. This page runs it live:

<ExampleEditor />

```tsx
import { Editor } from "@oh-just-another/editor";
import "@oh-just-another/react-ui/styles.css";

export const App = () => <Editor />;
```
