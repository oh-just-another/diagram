---
title: 🚧 Image Pipeline
status: draft
sidebar_position: 4
---

**Planned — not yet available.**

The Image Pipeline kit would chain image-generation and processing steps on the
canvas — each node a stage that takes images in, transforms them, and passes the
result downstream — so a visual graph drives a generation workflow.

This kit is not built yet. No image-pipeline-specific API exists today, so none
is documented here. The general capability of building and running node graphs
programmatically lives in `@oh-just-another/scene`; the **Agent** kit shows how a
process can construct and mutate scenes through real exports. This page will be
filled in once the kit is packaged.
