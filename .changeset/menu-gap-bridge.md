---
"@oh-just-another/react-ui": patch
---

Chrome distances tightened by 4px each (`--du-bar-inset` 12, `--du-dock-inset` 10, `--du-flyout-gap` 10, `--du-submenu-gap` 6). A nested main-menu panel now carries an invisible hover bridge over the gap to its parent column, so moving the pointer from the parent row into the child no longer closes it.
