---
"@oh-just-another/editor": minor
---

`<Editor>` accepts granular scene-settings props — `grid` (`{ enabled, style }`)
and `snap` — merged over the defaults. A persisted `initialScene` still wins over
them (user data over host config).
