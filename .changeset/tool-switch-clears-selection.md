---
"@oh-just-another/state": patch
---

Switching to a drawing, ink, eraser or laser tool clears the selection (so the selection toolbar goes away with it); `select`, `hand` and `crop` keep it. Leaving crop for another tool abandons the pending crop box.
