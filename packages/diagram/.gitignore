# Generated by `pnpm build:cdn` — wasm / font assets copied here so the CDN
# bundle's `../wasm` / `../fonts` URLs resolve at the package root. Source of
# truth lives in the text-wasm / raster-wasm / fonts packages.
/wasm/
/fonts/
