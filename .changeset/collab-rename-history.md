---
"@oh-just-another/collab": minor
---

Renamed `YjsHistory` / `YjsHistoryOptions` to `CollabHistory` / `CollabHistoryOptions`, so the public name describes the role (collaborative undo scoped to the local client) rather than the backing implementation.
