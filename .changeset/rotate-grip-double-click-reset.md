---
"@oh-just-another/state": minor
---

Double-clicking the rotate grip resets the selection's rotation to 0 (each element about its own centre, one undo step). New `Editor.resetSelectionRotation()`.
