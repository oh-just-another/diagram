---
"@oh-just-another/state": patch
---

Selected shapes no longer get a translucent contour halo under the body — the selection frame + handles are the only selection chrome. `paintElementSelectionHalo` / `ElementHalo` are removed (links keep their halo).
